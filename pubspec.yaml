name: plankp
description: PlanKP - Sistem Penjadwalan Maintenance Kencana Print
publish_to: "none"
version: 1.0.32+32 # Format: major.minor.patch+build

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  provider: ^6.1.2
  http: ^1.2.1
  dio: ^5.7.0
  package_info_plus: ^8.0.2
  path_provider: ^2.1.4
  shared_preferences: ^2.2.3
  flutter_secure_storage: ^9.0.0
  intl: ^0.19.0
  google_fonts: ^6.2.1
  awesome_dialog: ^3.2.1
  open_filex: ^4.5.0
  url_launcher: ^6.3.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/
